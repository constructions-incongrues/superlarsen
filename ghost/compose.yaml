services:

  ghost:
    image: ghost:5-alpine
    restart: always
    environment:
      # see https://ghost.org/docs/config/#configuration-options
      database__client: mysql
      database__connection__host: db
      database__connection__user: root
      database__connection__password: ${MYSQL_ROOT_PASSWORD:-example}
      database__connection__database: ghost
      # this url value is just an example, and is likely wrong for your environment!
      url: ${GHOST_URL:-http://localhost:2368}
      # contrary to the default mentioned in the linked documentation, this image defaults to NODE_ENV=production (so development mode needs to be explicitly specified if desired)
      #NODE_ENV: development
    volumes:
      - ghost:/var/lib/ghost/content
    networks:
      - caddy
      - default
    labels:
      caddy: superlarsen.interzone.network
      caddy.reverse_proxy: "{{upstreams 2368}}"

  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-example}
    volumes:
      - db:/var/lib/mysql

volumes:
  ghost:
  db:

networks:
  caddy:
    external: true
